# AGENTS.md

## CodeGraph 同步规则

- 全新仓库必须在仓库根目录执行 `codegraph init` 初始化 CodeGraph 仓库。
- 每次在实际仓库根目录拉取远端最新代码后，必须执行 `codegraph sync` 更新 CodeGraph 索引，并执行 `codegraph status` 确认索引为最新状态。
- 在提交或推送代码到远端之前，必须再次执行 `codegraph sync`，确保 CodeGraph 与最终待提交代码一致。
- 如果同步产生了 Git 可追踪的 CodeGraph 文件变更，应与本次代码变更一并提交；运行时临时文件仍以仓库内 `.codegraph/.gitignore` 的规则为准，不得强制加入 Git。

## 推送与 CI 关注

- 每次推送到远端后，必须定位本次推送对应的 CI run（`gh run list --branch main` 按 headSha 匹配，或 `gh run view <id>`）并关注至终态：结论 success 才视为推送完成；失败则查 job 定位原因、修复后再推送。上报时说明 CI 覆盖面边界（哪些 job 不跑、正确性靠本地验证兜底），不得把本地测试当 CI 结果上报。

## 编程原则

- **高内聚、低耦合**：相关逻辑集中，模块通过明确接口协作，避免依赖内部实现。
- **单一职责**：函数、类和模块只承担一类明确职责，按变化原因拆分。
- **开闭原则**：对稳定且反复出现的变化点提供扩展接口，避免为假设需求提前抽象。
- **依赖倒置**：核心业务不依赖具体框架或基础设施，通过必要的接口隔离。
- **接口隔离与里氏替换**：接口小而专一；替换实现不得破坏已有行为约定。
- **简单优先**：遵循 KISS、YAGNI；优先组合而非继承，不引入无必要的设计模式。
- **适度复用**：遵循 DRY，消除重复的业务规则，避免仅因代码相似而强行抽象。

## 架构设计

- **按需设计**：架构复杂度应与业务规模匹配，优先采用最简单、可演进的方案。
- **边界明确**：明确模块职责、公开接口和数据归属，避免跨模块访问内部状态。
- **依赖单向**：保持清晰的依赖方向，禁止循环依赖，避免绕过模块边界。
- **隔离副作用**：业务规则与数据库、网络、文件操作分离，便于独立理解和验证。
- **契约清晰**：明确输入、输出、错误和兼容性要求；数据在系统边界校验。
- **状态可控**：减少共享可变状态，让状态变化显式且可追踪。

## 实施要求

- 修改前理解现有代码和约定，围绕当前需求做最小必要变更。
- 命名准确，控制嵌套和函数复杂度；注释解释原因、约束与取舍。
- 显式处理错误，不吞异常，不用默认值掩盖失败；日志应便于定位且不泄露敏感信息。
- 不硬编码密钥；外部输入不可信，数据库查询使用参数化方式。
- 按变更风险验证关键行为、边界和失败路径；测试关注行为，避免绑定内部实现。
- 新增依赖或架构层次前说明必要性；性能优化应有测量依据。
- 交付时简要说明改动、验证结果和已知限制，不声称完成未执行的检查。

## 取舍准则

原则冲突时，优先保证正确性和可读性，再结合实际变化需求选择方案，避免机械套用。

