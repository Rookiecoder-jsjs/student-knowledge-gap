# DELTA —— 相对锚点的全部源码分歧台账

> 性质：魔改仓库与上游的**每一处**源码分歧都在此登记（位置/内容/原因），
> ADR 条目式追加，永不重写历史条目。任何人 diff 我们的 runtime/ 与上游锚点时，
> 每一处差异都应能在本文件找到对应行——反之亦然（账外分歧 = 缺陷）。
>
> 维护纪律：第一笔源码修改起逐笔开账；cherry-pick 上游修复不产生新条目
> （那是向锚点收敛），但若 cherry-pick 与既有分歧冲突需手工对位时补记一条。

## 锚点（2026-08-24 登记）

| 项 | 值 |
|---|---|
| 上游仓库 | openai/codex（Apache-2.0） |
| 锚定 tag | `rust-v0.149.1` |
| 锚定 commit | `ff29a44391deccde0aba0f8390337d7f3c319ea4`（2026-08-24 发布当日 latest stable） |
| 收编方式 | 本地克隆 checkout 到锚点 → 去 `.git` 整体搬入为 `runtime/`（§2.1 铁律：内部一字不动） |
| 收编日期 | 2026-08-25 |

## 分歧台账

| # | 位置 | 内容 | 原因 | 日期 |
|---|---|---|---|---|
| D-001 | `codex-rs/Cargo.toml` `[workspace.members]` | 移除 `"v8-poc"`、`"code-mode-host"`、`"thread-manager-sample"` 三项 | §6.1 裁剪（CRATES.md P1-1 批）；三 crate 零反向依赖，目录一并删除 | 2026-08-25 |
| D-002 | `codex-rs/Cargo.toml` `[workspace.dependencies]` | 移除 `codex-v8-poc = { path = "v8-poc" }` | 同上（随成员删除的声明清理） | 2026-08-25 |
| D-003 | `codex-rs/Cargo.toml` `[workspace.metadata.cargo-shear]` ignored | 移除 `"codex-v8-poc"` | 同上 | 2026-08-25 |
| D-004 | `codex-rs/Cargo.toml` members + deps | 移除 cli / tui / cloud-tasks / cloud-tasks-client / cloud-tasks-mock-client 五成员及 `codex-cloud-tasks-client/-mock-client/codex-tui` 三条 workspace.deps | §6.1 前门批（P1-2）：产品唯一前门 = app-server；验证 app-server 949 测试全绿（CRATES.md P1-2 行） | 2026-08-25 |
| D-005 | `codex-rs/app-server/tests/suite/v2/{mod.rs, code_mode_host.rs, executor_mcp.rs, selected_capability_stack.rs, turn_start.rs, imagegen_extension.rs}` | 删除三个测试文件 + 两个用例（均依赖已删除的 codex/cli 二进制或 code-mode-host 执行面） | AGENTS-FORK 纪律：不为已删逻辑保留测试；随 D-004 连带退役 | 2026-08-25 |
| D-006 | `codex-rs/Cargo.toml` members + deps | 移除 code-mode / code-mode-protocol / code-mode-runtime 三成员及 workspace.deps | §6.1 code-mode 手术批（CRATES.md P1-3a）：DeepSeek 单模型下执行面永不激活；连带移除 Feature/工具路由/单元格追踪等 342 处引用，workspace 不再拉 V8 | 2026-08-25 |
| D-007 | `codex-rs/Cargo.toml` members + deps | 移除 connectors / ext/connectors 两成员及 workspace.deps | §6.1 connectors 手术批（CRATES.md P1-3b）：58 处引用全部清除（ConnectorRuntimeManager 缓存通道、ConnectorSnapshot、AppToolPolicyEvaluator 内联、parse_plugin_app_config 迁入 codex-plugin、app 认证 elicitation 流程、DiscoverableTool::Connector 变体） | 2026-08-26 |
| D-008 | `codex-rs/codex-mcp/src/{rmcp_client.rs, connection_manager.rs, tool_catalog.rs, runtime.rs, lib.rs, resource_origin.rs, auth_elicitation.rs}` | 拆除 Codex Apps 工具缓存通道（McpRuntimeInput/AsyncManagedClient 字段、cached_server_info、hard_refresh 发布、类型别名）；auth_elicitation 缩为元数据键常量 | 随 D-007 连带：缓存通道仅服务 ChatGPT apps 认证，教育产品永不激活 | 2026-08-26 |
| D-009 | `codex-rs/core/src/{mcp_tool_call.rs, mcp_tool_exposure.rs, connectors.rs, session/world_state.rs, session/mod.rs, session/mcp.rs, mcp.rs, tools/spec_plan.rs}` | 移除 app 认证 elicitation 调用、apps 工具暴露分支（append_mcp_tools 简化为纯 MCP）、connectors 列表/缓存模块（仅留审批 reviewer）、Apps 使用说明 world-state 段、selected_plugin_connector_sources 死代码链、build_tool_router 的 apps_enabled 参数 | 随 D-007 连带：apps/connectors 暴露面全量清除 | 2026-08-26 |
| D-010 | `codex-rs/{tools, core-plugins, ext/mcp, ext/extension-api, plugin}` | tools 移除 DiscoverableTool::Connector 变体与 connector 校验函数；parse_plugin_app_config 迁入 codex-plugin；ext/mcp 移除 connector 声明加载与 SelectedPluginPackage.connector_ids 字段；plugin 新增 app_config 模块 | 随 D-007 连带：连接器声明/策略/加载链路迁至保留 crate 或删除 | 2026-08-26 |
| D-011 | `codex-rs/app-server/tests/suite/v2/{plugin_install.rs, recommended_plugins.rs}` | plugin_install 三个 remote-oauth 用例补回 `mount_remote_plugin_install` mock（上批删 with_apps_needing_auth 时误删）；recommended_plugins 移除 request_plugin_install 工具断言 | 修复 D-007 上批遗留红测试；request_plugin_install 工具已随 tool_suggest 裁剪 | 2026-08-26 |
| D-012 | `codex-rs/core/src/{plugins/{render.rs, render_tests.rs, injection.rs}, context/{plugin_instructions.rs, mod.rs}, session/turn.rs}` | 恢复插件显式提及能力段：`render_explicit_plugin_instructions`（去 apps 分支）、`build_plugin_injections`（去 apps 参数）、`PluginInstructions` 片段，并接回 `build_skills_and_plugins` | 修复上批过度删除：code-mode 批删 render/injection 时把普通插件（技能/MCP 服务器）的能力段一并删了，skills_extension 三个测试因此红 | 2026-08-26 |
| D-013 | `codex-rs/core/tests/suite/{cli_stream.rs, request_plugin_install.rs, mcp_turn_metadata.rs, mcp_auth_elicitation.rs}`（整文件删除）+ client/mcp_tool_exposure/openai_file_mcp/plugins/rmcp_client/codex_delegate/skills_extension 各 apps/CLI 用例 | 退役 35 个已删功能的过期测试：CLI 二进制（P1-2 删）、request_plugin_install 工具（tool_suggest 删）、apps 审批路由/apps 指南/apps 工具暴露/codex_apps 文件参数/apps 认证 elicitation 等 | AGENTS-FORK 纪律：不为已删逻辑保留测试；确定性失败（隔离亦红）经 HEAD worktree 对照确认全部指向已删功能 | 2026-08-26 |
| D-014 | `codex-rs/core/tests/suite/{agent_execution.rs, rmcp_client.rs, client.rs}` | 修三个遗留编译/断言问题：v2_residency 断言从 code-mode markdown spec 改为工具名、`McpCallEvent` 补回被误删的 derive、`ProviderAuthCommandFixture` 补回被误删的 `#[cfg(unix)]/#[cfg(windows)]` | 上批误删连带；restore 后全绿 | 2026-08-26 |
| D-015 | `codex-rs/Cargo.toml` members + deps | 移除 `"bwrap"`、`"linux-sandbox"` 两成员及 `codex-linux-sandbox` workspace.dependency；目录整体删除（含 `vendor/bubblewrap` vendored C 源码） | §6.1 沙箱批第一批（CRATES.md P1-4a）：Linux OS 沙箱面，workspace 不再编译 landlock/seccomp/seccompiler/bubblewrap 链 | 2026-08-26 |
| D-016 | `codex-rs/sandboxing/src/{manager.rs, lib.rs, violation.rs, denial.rs}` | 门面降级：`SandboxType` 删 `LinuxSeccomp` 变体；`get_platform_sandbox` linux→None；`SandboxTransformRequest` 删 `codex_linux_sandbox_exe`/`use_legacy_landlock` 两字段；删 `MissingLinuxSandboxExecutable`/`Wsl1UnsupportedForBubblewrap` 错误变体；删 `landlock`/`bwrap` 模块与 re-export；`SandboxViolationBackend::LinuxSandbox` 变体与 SIGSYS 探测删除（`SystemError::LandlockSandboxExecutableNotProvided` 同步从 protocol 移除） | 随 D-015 连带：门面仅保留 macOS seatbelt + Windows restricted-token（后者后续批）；Linux 上 `Require` 偏好的 fs helper/沙箱请求 fail-closed 拒绝 | 2026-08-26 |
| D-017 | `codex-rs/{arg0, core, exec-server, app-server, exec, codex-mcp, mcp-server, app-server-client, exec-server-protocol, file-system, codex-config, features, utils/cli}` | 清除 `codex_linux_sandbox_exe` 与 `use_legacy_landlock` 全链路：arg0 派发/别名/`Arg0DispatchPaths` 字段、`Config`/`ConfigOverrides`/`McpConfig`/`SandboxState`/`FileSystemSandboxContext`/`CoreShellCommandExecutor` 字段、`ExecServerRuntimePaths` 单参化、`ProcessSandboxType::LinuxSeccomp` wire 变体；`features` 删 `Feature::UseLegacyLandlock`(Deprecated) 与 `Feature::UseLinuxSandboxBwrap`(Removed) 两标志 + apply_map 别名 arm；`config.schema.json` 随 FEATURES 重新生成（含 `codex-write-config-schema` 输出） | 随 D-015/D-016 连带：Linux 沙箱配置/路径面全量清除；删除后旧配置含 `[features] use_legacy_landlock=true` 报未知 key（行为变更，可接受） | 2026-08-26 |
| D-018 | `codex-rs/{core/BUILD.bazel, app-server/BUILD.bazel, exec-server/BUILD.bazel}` | 移除 `//codex-rs/bwrap`、`//codex-rs/linux-sandbox` 引用 | Bazel 双轨同步（D8 义务）：随 crate 删除清引 | 2026-08-26 |
| D-019 | `codex-rs/{exec/tests/suite/sandbox.rs, exec-server/{src/{process_sandbox_tests.rs, fs_sandbox.rs} 测试段, tests/{exec_process.rs, file_system_unix.rs, common/mod.rs}}, core/{src/{exec_tests.rs, unified_exec/process_tests.rs, tools/{sandboxing.rs, orchestrator.rs, runtimes/*}, session/*, agent/role_tests.rs, guardian/review_session.rs 等} 测试字面量, tests/{suite/*, common/lib.rs}}}` | 退役 linux OS 沙箱过期测试：exec sandbox 套件 5 用例 + 2 linux 探测、arg0 `linux_sandbox_exe_path`、fs_sandbox bwrap 用例、hostile-helper 用例、`find_codex_linux_sandbox_exe`/`codex_linux_sandbox_exe_or_skip!` 测试基建、use_legacy_landlock deprecation 用例、SIGSYS 探测用例；余下字面量字段删除 | AGENTS-FORK 纪律：不为已删逻辑保留测试；linux 沙箱删除后相关断言失去语义（Linux 上已无沙箱可断） | 2026-08-26 |
| D-020 | `codex-rs/{handbook/ARCHITECTURE.md}`（文档列） | update bwrap/linux-sandbox crate 描述以反映删除 | 文档随裁剪同步（handbook 单一事实源） | 2026-08-26 |
| D-021 | `codex-rs/Cargo.toml` members + deps | 移除 `windows-sandbox-rs`（path 依赖隐式成员，非显式 member）与 `codex-windows-sandbox` workspace.dependency；目录整体删除（约 60 个 Windows 目标门控源文件与 BUILD.bazel）；连带清 root/network-proxy/core/sandboxing/app-server/arg0 六处 manifest 依赖行 | §6.1 windows-sandbox 拆除（CRATES.md P1-4b）：纯 Windows 面，容器化/Linux 部署永不激活，磁盘零增益（目标门控）但依赖图 ledger 收口 | 2026-08-27 |
| D-022 | `codex-rs/sandboxing/src/{manager.rs, lib.rs, spawn.rs, violation.rs}` + 删 `windows.rs` | 门面降级：`SandboxType` 删 `WindowsRestrictedToken` 变体；`get_platform_sandbox` 去参数、windows 条件臂→None；`SandboxExecRequest`/`SandboxTransformRequest` 删 `windows_sandbox_level`/`windows_sandbox_private_desktop` 两字段；删 `SandboxDirectSpawnTransformRequest`/`transform_for_direct_spawn`/`spawn::WindowsSandboxSpawnRequest`/`SpawnRequest.windows_sandbox`/`WindowsSandboxFilesystemOverrides`/`WindowsSandboxProxySettingsMode` re-export/`SandboxViolationBackend::WindowsSandbox`/`SandboxTransformError::WindowsSandboxPreparation`；manager_tests 同步修字段、退役两个 cfg(windows) 用例 | 随 D-021 连带：门面仅剩 macOS seatbelt；Windows 上 `Require` 偏好 fs helper/exec 沙箱请求 fail-closed 拒绝 | 2026-08-27 |
| D-023 | `codex-rs/exec-server-protocol/src/protocol.rs` + `codex-rs/exec-server/src/{process_sandbox.rs, local_process.rs, process.rs, fs_sandbox.rs, capability_discovery.rs}` | 强制面清除：`ProcessSandboxType::WindowsRestrictedToken` wire 变体删除；`PreparedWindowsSandboxRequest`/`windows_sandbox_spawn_request()` 删除；`select_initial`/`transform`/`SandboxTransformRequest` 调用点去 windows 字段；`sandbox_exec_request` 收窄为两参数；capability_discovery 去 windows level 检查；退役 windows 沙箱 gated 测试（process_sandbox_tests 的 managed_network 用例、file_system_windows.rs 沙箱用例+helper、exec_process.rs windows 写用例、common/mod.rs 的 windows sandbox 派发） | 随 D-021/D-022 连带：exec-server 不再 spawn Windows 沙箱 session | 2026-08-27 |
| D-024 | `codex-rs/features/src/{lib.rs, legacy.rs}` | 删除 `Feature::WindowsSandbox`/`Feature::WindowsSandboxElevated` 两变体（均已 Stage::Removed）与对应 FeatureSpec 两条、legacy alias `enable_experimental_windows_sandbox` | 随 D-021 连带：windows 沙箱标志失去语义；旧配置含 `--enable windows_sandbox` 报未知 feature（行为变更，可接受） | 2026-08-27 |
| D-025 | `codex-rs/config/src/{types.rs, config_toml.rs, profile_toml.rs, config_requirements.rs}` | 配置面清除：删 `WindowsSandboxModeToml`/`WindowsToml`（`[windows]` 段）；`ConfigToml`/`ProfileConfig` 删 `windows` 字段；`derive_permission_profile` 删 `windows_sandbox_level` 参数与 Windows 降级分支；ConfigRequirements 删 `windows_sandbox_mode`/`windows_sandbox_private_desktop` 两字段、`WindowsRequirementsToml` 结构体、解析块、managed-fields 条目 | 随 D-021 连带：requirements `[windows]` 段与 config `[windows]` 表失去解析；旧配置含之报未知 key（行为变更，可接受） | 2026-08-27 |
| D-026 | `codex-rs/protocol/src/config_types.rs` | 删两类型定义：`WindowsSandboxLevel` 与 `WindowsSandboxProxySettingsMode`；`ThreadSettingsOverrides` 删 `windows_sandbox_level` 字段 | 随 D-021 连带：配置/protocol 类型面收口（STEP 5 类型删除，消费者先清） | 2026-08-27 |
| D-027 | `codex-rs/file-system/src/lib.rs` | `FileSystemSandboxContext` 删 `windows_sandbox_level`/`windows_sandbox_private_desktop`/`windows_sandbox_proxy_settings_mode` 三字段及构造/读取点 | 随 D-021/D-022 连带：fs helper 沙箱上下文不再携带 Windows 级别/桌面/代理设置 | 2026-08-27 |
| D-028 | `codex-rs/core/src/**`（大面）+ 测试 | 删除核心窗模块：`windows_sandbox.rs`/`windows_sandbox_tests.rs`/`windows_sandbox_read_grants.rs`(+tests)/`exec_policy_windows_tests.rs`/`tests/suite/windows_sandbox.rs`；清除跨面 plumbing：exec.rs/exec_policy.rs/sandboxing(模块)/tools(SandboxAttempt.executor_windows_sandbox_level、orchestrator、runtimes)/session(TurnContext/Session/SessionUsageConfiguration/McpDesiredState/ThreadSettingsOverrides/CodexThreadSettingsOverrides)/agents_md(+manager)/safety/codex_delegate/thread_manager/guardian/config(mod/permissions/edit/requirements)/sandbox_tags/unified_exec 的 `windows_sandbox_*` 字段与 `SandboxType::WindowsRestrictedToken` 引用、PS-profile 禁用助手；同步退役死测试并修 `ShellRequest`/`RunExecLikeArgs` 的 `shell_type` 死字段链（唯一消费者为已删 windows 降级函数） | 随 D-021 连带：core 全窗代码退出，`SelectInitial` 等门面调用去 windows 参数 | 2026-08-27 |
| D-029 | `codex-rs/app-server/src/**` + `app-server-protocol/src/**` | 产品面清除：删 `request_processors/windows_sandbox_processor.rs`、`tests/suite/v2/windows_sandbox_setup.rs`、`app-server-protocol/src/protocol/v2/windows_sandbox.rs`；message/request 接线、`command_exec.rs`（删 `CommandExecSession::UnsupportedWindowsSandbox` 变体与 windows exec 路径，修两处 irrefutable 匹配）、`command_exec_processor.rs`/`config_processor.rs`/`thread_processor.rs`/`turn_processor.rs`/`feedback_processor.rs` windows 处理；protocol common.rs/v2 config.rs 的 windows 请求/通知/字段；schema typescript/json Windows*.ts/json 随 build.rs 再生删除（+precomputed .zst 经 schema_fixtures 脚本重写） | 随 D-021 连带：app-server 的 windows-readiness/Setup UX 整体下线 | 2026-08-27 |
| D-030 | `codex-rs/network-proxy/tests/windows_stable_ingress.rs`（删除）+ app-server `command_exec.rs`/core `tools/**` 收尾修复 | windows 沙箱 ingress 集成测试整文件删除（`#![cfg(target_os = "windows")]`，依赖已删 crate）；agent 收尾后补修我侧确认的两类遗留：`command_exec.rs` 两处 irrefutable 匹配、`ShellRequest.shell_type`/`RunExecLikeArgs.shell_type` 死字段链（唯一消费者是 D-028 删的 windows 降级函数） | 随 D-021 连带：无引用已删 crate 的残留；简历未引入新 warning（保留唯一 HEAD 既有 `review_context` 潜在 warning） | 2026-08-27 |
| D-031 | `codex-rs/{BUILD.bazel(根), app-server/BUILD.bazel, core/BUILD.bazel, rmcp-client/BUILD.bazel}` + `justfile` | Bazel 轨悬空引用清理与 release 构建重指：① `extra_binaries`/`extra_binaries_non_windows` 移除 P1-1 已删 `//codex-rs/code-mode-host:codex-code-mode-host`（core/app-server 两处）与 P1-2 已删 `//codex-rs/cli:codex`（core/app-server/rmcp-client 三处），app-server 空掉的 `extra_binaries_non_windows` 参数一并删；② 根 `BUILD.bazel` 退役 `e2e-benchmarks` test_suite（引用已删 `//codex-rs/cli:codex-help-bench`）；③ `justfile` `build-for-release` 由 `//codex-rs/cli:release_binaries`（已删）改指 `--compilation_mode=opt //codex-rs/app-server:codex-app-server //codex-rs/app-server:exec-server`；④ `justfile` 退役 `bench-e2e`/`bench-e2e-smoke`（随 e2e-benchmarks） | P1-1/P1-2 裁剪只清 Cargo 轨漏清 Bazel 轨（违 D8 双轨义务，账外残留转正账）；fork 的 Bazel 轨自 P1-1 起不可构建，CI 从不跑 Bazel 未暴露；本批为装车铺路——产品壳二进制 = `codex-app-server`（独立二进制，网关对接面），`exec-server` 随 app-server crate 同出（arg0 兄弟二进制解析）；上游遗留 `.github/` CI 与 `codex-rs/docs/bazel.md` 的 cli 引用不随本批清理（fork 不跑上游 CI，惰性遗留） | 2026-09-01 |
| D-032 | `codex-rs/MODULE.bazel` `single_version_override(rules_rs)` + `patches/rules_rs_zlib_deb_url.patch`（新增） | 给 rules_rs@0.0.96 追加补丁，将 `rs/private/rustc_repository.bzl` `_LINUX_ZLIB` 的 aarch64/x86_64 两处 zlib deb URL+sha256 由 `zlib1g_1.3.dfsg-3.1ubuntu2.1` 升到 `-3.1ubuntu2.2`；`MODULE.bazel.lock` 随 `bazel mod deps --lockfile_mode=update` 同步 | Ubuntu 归档清理了 `-3.1ubuntu2.1` deb（404），rules_rs 工具链引导（rustc 内嵌 libLLVM 需 libz.so.1）在任何环境都拉不到 → Bazel 构建挂；升到现存 `.2` 版本（2026-09-02 实测 sha256） | 2026-09-02 |
| D-033 | `codex-rs/sandboxing/src/manager.rs:335` | `transform` 中 `pending_sandboxed_request.map_or(...)` 闭包参数补显式类型 `|pending: PendingSandboxedExecRequest|` | P1-4a（D-016）删 `LinuxSeccomp` 变体后，Linux 上 `transform` 的 match 不再产出 `Some(pending)`，`Option` 元素类型仅由后续字段访问约束 → E0282 类型推断失败；macOS 开发机（Seatbelt 分支仍产出 `Some(pending)`）Cargo 验证从未暴露。生产目标 Linux，Bazel opt 构建必炸。显式标注修复 | 2026-09-02 |
| D-034 | `codex-rs/school-authz/`（新 crate）+ `codex-rs/Cargo.toml`（members 加 `"school-authz"`、workspace.dependencies 加 `codex-school-authz`）+ `Cargo.lock`（新成员条目，依赖复用既有 hmac/sha2/thiserror）+ `codex-rs/school-authz/BUILD.bazel` + `codex-rs/MODULE.bazel.lock`（随 `bazel mod deps --lockfile_mode=update` 同步）+ `justfile`（`build-for-release` 追加 `//codex-rs/school-authz:school-authz-mcp`） | §6.3 加批首笔（CRATES.md 预定新增）：教师身份 token 校验 + `assert_class_access` 权限断言原语 + `school-authz-mcp` stdio MCP shim——从签名 token 派生并注入 `SC_MCP_TEACHER_ID`，替代「网关注入裸 SC_MCP_TEACHER_ID env、后端无条件信任」的 §5.5 兜底路线。token 格式与 sc 后端 `backend/app/auth.py` 逐字节一致（`teacher_id.exp.sig`，HMAC-SHA256 hex，同密钥 `SC_AUTH_SECRET`）。网关签发、shim 验、后端已能验，单一密钥零新概念。官方壳无此能力 = runtime 首笔差异化价值。零新增外部依赖（hmac 0.12.1/sha2 0.10/thiserror 均已在 workspace.dependencies） | 2026-09-02 |
| D-035 | 装车批第 5 批（sc 侧 + fork 工程，零 fork 源码分歧）：`backend/app/{mcp_http.py 新增, auth.py mcp_context contextvar, mcp_server.py FastMCP transport_security, main.py /mcp 挂载}`；`gateway/main.py`（`_child_env` env 白名单、trigger 按提交教师分桥 + `TriggerReq.teacher_id`、TTL→7d）；`gateway/{codex_home.py, assets/deepseek/config.toml.template}` 改远程 url+bearer；`gateway/{Dockerfile, requirements.txt 新增}` 自包含；`deploy/{docker-compose.yml gateway 卸 sc-data, stage-gateway-runtime.sh 只 stage 两枚}` | **容器级隔离**（第 3 步显式声明的暴露面收紧）：sc MCP 迁入 backend 进程（streamable-http 挂 `/mcp`，与 REST 共 engine——「第二 SQLite 访问者」例外消除）；gateway 卸载 sc-data 卷，agent 壳物理不可达 sc.db；身份改逐请求 `Authorization`（backend 验签），`school-authz-mcp` 停止 stage/部署、crate 保留作参考实现——D-034 的差异能力迁为「backend 逐请求教师授权」（官方 codex 支持 url MCP + bearer_token_env_var，差异点在数据属主侧授权）；自动考后 trigger 按提交教师实名驱动（修掉匿名在安全模式读不了班的既有缺口）；agent 子进程 env 白名单化（共享密钥不进） | 2026-09-02 |

验证：`cargo check -p codex-app-server` 通过（产品二进制依赖闭包不受影响）；
app-server 测试 949 passed 全绿。已知上游自带红测试：
`environment_selection::tests::blocking_snapshot_waits_for_starting_environment`
（锚点 worktree 对照复现，非 fork 引起）。

## 上游参考途径（D10，codex/ 参考克隆已删）

1. **GitHub 在线看**：`https://github.com/openai/codex/blob/rust-v0.149.1/<path>`
2. **本地 grep**：临时克隆到仓库外任意位置（如 `~/refs/codex`）
   `gh repo clone openai/codex ~/refs/codex -- --branch rust-v0.149.1`
3. **行为对比测试**：按锚点 commit 克隆跑上游测试
   `gh repo clone openai/codex ~/refs/codex && git -C ~/refs/codex checkout ff29a44391`

跨仓 cherry-pick 配方（§2.1 铁律三）：上游克隆 `git format-patch` 出补丁 →
本仓库 `git am --directory=runtime` 应用。
