# Optional: attach your Prometheus instance to the Compose network.
global:
  scrape_interval: 30s
rule_files:
  - alerts.yml
scrape_configs:
  - job_name: sc-backend
    static_configs:
      - targets: ["backend:8000"]
